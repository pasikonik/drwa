{
  "name": "drwa-frontend",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "NODE_OPTIONS=--dns-result-order=ipv4first nuxt build",
    "dev": "NODE_OPTIONS=--dns-result-order=ipv4first nuxt dev",
    "generate": "NODE_OPTIONS=--dns-result-order=ipv4first nuxt generate",
    "preview": "NODE_OPTIONS=--dns-result-order=ipv4first nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@directus/sdk": "^24.0.0",
    "@stripe/stripe-js": "^9.13.0",
    "nuxt": "^4.5.2",
    "resend": "^6.20.0",
    "stripe": "^22.5.0",
    "vue": "^3.5.41",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
